{
	"name": "@beholder/worker",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"description": "Beholder API — auth, event sync, backups (Cloudflare Workers + D1). Built in milestone M4.",
	"scripts": {
		"test": "vitest run --passWithNoTests"
	},
	"devDependencies": {
		"vitest": "^4.0.18"
	}
}
