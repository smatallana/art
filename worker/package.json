{
	"name": "@beholder/worker",
	"private": true,
	"version": "0.1.0",
	"type": "module",
	"description": "Beholder API — auth, event sync, export/delete (Cloudflare Workers + D1).",
	"scripts": {
		"test": "vitest run",
		"check": "tsc --noEmit",
		"deploy": "wrangler deploy"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260501.0",
		"typescript": "^5.9.3",
		"vitest": "^4.0.18",
		"wrangler": "^4.48.0"
	}
}
