# Decision log

Running record of product and engineering decisions. Newest last.

## 2026-07-27 — Discovery (5 rounds with the owner)

| Area | Decision |
|---|---|
| Users | Multiuser from v1 + fully-local guest mode |
| Infrastructure | $0/month sustained on managed free tiers; GitHub is the source of truth |
| Repo/deploy | Public repo + GitHub Pages (`smatallana.github.io/art`) |
| Auth | Google OAuth only in v1; guest → account upgrade migrates local events |
| Starting profile | Owner's ~40 prior manual comparisons imported as a low-confidence prior, explicitly designed to be refuted |
| Language | English UI; i18n-ready architecture for adding Spanish later |
| Scope | Painting only; ontology/data model extensible to other visual arts |
| Product stance | Public, non-commercial → catalog restricted to public-domain works with CC0 metadata |
| Devices | iPhone Safari first; desktop second; Android verified |
| Motivation | Soft in-app continuity, no push notifications, no pressure streaks |
| Social | None in v1 (backlog) |
| Domain | Default Pages URL |
| Education level | Medium: micro-stories after reveals + context in explore |
| Museum mode | Backlog (data model keeps museum/collection per work) |
| Rating | One main preference scale + optional emotional reactions |
| Analytics | None (only user-visible technical error diagnostics) |
| Name | **Beholder** ("Art Genome" rejected: collision with Artsy's Art Genome Project) |
| Design direction | **A: Night gallery** (chosen over B: Printed catalogue) |
| Snap | Photo capture of real-world paintings added to v1 scope (M5) at owner's request |
| Cloudflare | Owner confirmed creating the free account + adding 2 repo secrets |

## 2026-07-27 — Architecture (research-driven)

Four official-source research reports (museum APIs/licensing, iOS PWA state 2026, backend free
tiers, GitHub limits) informed these choices — summarized in ARCHITECTURE.md:

- **Hybrid GitHub + Cloudflare** chosen over (a) fully-static local-first (no accounts/sync)
  and (b) BaaS (Supabase Free pauses after 1 week idle; Appwrite Free pauses at 7 days and
  deletes at 90; Firebase = heavy lock-in, no free functions).
- **Cloudflare Workers + D1** for auth/sync: no pause behavior of any kind on the free tier,
  no credit card, `wrangler d1 export` → standard SQL as the exit hatch.
- **Cloudflare R2** for image derivatives: 10 GB + free egress; museum hotlinking is not
  reliable (Met is anti-bot protected; only AIC explicitly sanctions IIIF hotlinking).
- **Catalog sources v1:** Art Institute of Chicago, Cleveland Museum of Art, The Met,
  Rijksmuseum — all CC0/public-domain with official bulk data. Harvard rejected
  (non-commercial images, 2-week cache cap); WikiArt rejected (unlicensed aggregation);
  Wikidata used for cross-source dedup/enrichment only.
- **Model computed client-side** from an append-only event log (event sourcing): thin $0
  backend, strong privacy, model upgrades replay history.
- **SvelteKit + adapter-static**: smallest runtime for the 4G-fast-load budget; built-in
  motion primitives cover the "high-end microinteractions" requirement without extra deps.

## 2026-07-27 — M0 implementation notes

- SPA deep links on Pages use the `404.html` fallback (adapter-static `fallback` option).
- PWA icons are committed (generated by `app/scripts/icons.mjs`) for deterministic builds.
- Playwright E2E runs iPhone-14 WebKit and desktop Chromium; WebKit installs in CI
  (locally only Chromium is preinstalled).
- Service worker runtime caches are capped (600 artwork images, 60-day TTL, purge on quota
  error) so iOS storage pressure never breaks the app.
